[build]
  publish = "public"
  functions = "netlify/functions"
  command = "node scripts/prepare-fonts.mjs"console.log('Mario Chess + Supabase : build prêt')\""

[build.environment]
  NODE_VERSION = "22.13.0"

[functions]
  node_bundler = "esbuild"

[[headers]]
  for = "/api/*"
  [headers.values]
    Cache-Control = "no-store"
    X-Content-Type-Options = "nosniff"
